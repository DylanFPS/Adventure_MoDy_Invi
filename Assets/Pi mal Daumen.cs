using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PimalDaumen : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Start("Pimaldaumen_pasjhdjk");
    }

    // Update is called once per frame
    void Update()
    {
        end(dididi);
    }
}
